/**
 * Manual html configuration
 */

module.exports = {
  // Use GZip for answer from server
  gzip: false,
  // uglifying js files
  uglify: true,
  // cache response by requests
  cache: false,
  // compress css file into one
  cssCompression: false,

};